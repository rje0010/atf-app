export class CardModel {
    img: string;
    imgTitle: string;
    imgText: string;

    constructor(img: string, imgTitle: string, imgText: string){
        this.img=img;
        this.imgTitle=imgTitle;
        this.imgText=imgText
    }
}